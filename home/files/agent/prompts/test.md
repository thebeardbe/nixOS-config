---
description: Have the test-writer add and run tests for a change
argument-hint: "<what to test>"
---
Use the `subagent` tool (single mode, agent `test-writer`) to add and run tests
for the following. The builder must not be involved and must not see the tests.

Target: $@

Do not fix or change production code yourself. Report the test-writer's
Tests added, Result and Findings verbatim, and do not paste test source into a
builder task.
