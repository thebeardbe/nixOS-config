---
description: Delegate an implementation task to the builder subagent
argument-hint: "<task>"
---
Use the `subagent` tool (single mode, agent `builder`) to implement this task.
Do not implement it yourself.

Task: $@

Pass any relevant file paths and constraints. When it returns, show its
Summary, Files changed and Evidence, then give the user a short manual test
plan. Do not paste test files or test names into the builder's task.
